#include <bits/stdc++.h>
using namespace std;

int main()
{
    int tc;
    cin >> tc;
    while (tc--)
    {
        int n, k;
        cin >> n >> k;
        int num[n], ans = INT_MIN, s;
        for (int i = 0; i < n; i++)
        {
            cin >> num[i];
        }
        ans = max(ans, s);
        
    }
    return 0;
}